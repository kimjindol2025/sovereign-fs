// Phase 2: Deduplication Module

pub mod dedup_engine;

pub use dedup_engine::{
    DedupEngine, SimilarityScore, SimilarContent,
    DedupStrategy, DedupRecommendation, DedupStats,
};

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_dedup_module_exports() {
        let engine = DedupEngine::new();
        assert_eq!(engine.manifests.len(), 0);
    }
}
